import styles from './Footer.module.scss';

function Footer() {
  return (
    <footer className={styles.footer}>
      <p>Hecho con 💙 usando SASS y CSS Modules</p>
    </footer>
  );
}

export default Footer;
